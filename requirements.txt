# Core orchestration
langgraph>=0.2

# Browser automation + screen recording
playwright>=1.44

# LLM for script generation (1 call per pipeline run)
google-genai>=1.0

# API server
fastapi>=0.110
uvicorn[standard]>=0.29

# Config / env
python-dotenv>=1.0
pyyaml>=6.0          # demo_config.yaml loader for standalone agent mode

# Windows certificate store integration
# Makes Python trust the same CAs as Windows (including corporate proxy certs).
truststore>=0.9

# TTS voice-over — free, no API key, no quota
edge-tts>=6.1

# Logging
structlog>=24.1

# Testing
pytest>=8.0
httpx>=0.27   # required by FastAPI's TestClient

# NOTE: FFmpeg is a system dependency, not a pip package.
# Install separately — see docs/INSTALL.md for instructions.
