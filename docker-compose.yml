services:
  demo-video-bot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Output files land on the host so they survive container restarts.
      - ./output:/app/output
